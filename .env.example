# === Build provenance for runtime ===
# CI での core/logic の由来。単一リポジトリなので通常は同一 SHA で問題ありません。
CORE_GIT_SHA=
LOGIC_GIT_SHA=

# runtime 自身のコミット（未指定ならスクリプト/ビルドで自動補完）
RUNTIME_GIT_SHA=

# CI の Run ID（ローカルでは任意。未指定なら "local-<YYYYmmddHHMMSS>" などに補完）
GITHUB_RUN_ID=